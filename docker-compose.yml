services:
  web:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - ./:/app
      - /app/node_modules
    env_file:
      - .env.local
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: npm run dev
